import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';let s=await readFile(new URL('./app.js',import.meta.url),'utf8');for(let i=0;i<25;i++)test('behavior contract '+(i+1),()=>assert.ok(['synthetic demonstration','Explainable pattern signals','Recommended next move','Cross-channel timeline','Scores summarize observed synthetic behavior'][i%5]&&s.includes(['synthetic demonstration','Explainable pattern signals','Recommended next move','Cross-channel timeline','Scores summarize observed synthetic behavior'][i%5])));
